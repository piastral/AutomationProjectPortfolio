package testNgPackage1.MavenTestNgProject1;

public class TestNgXml {
	/*i organzaition we dont need to create so many classes 
	 * we can create two suites one is :: smoke testing suite
	 * and another one is regression testing suite ::
	 * because only these two testing we will perform for selenium 
	 * 
	 * Now Test ng xml file 
	 * ::NAVIGATING to create Xml file::
	 * Right click on the package :: ->New ->Click on Other ->Xml ->Xml file -> SPecify the name with .xml
	 * ->click on ok ->click on source view on xml file -> prepare the xl program like below
	 * 
	 * 
	 * 
	 */

}
