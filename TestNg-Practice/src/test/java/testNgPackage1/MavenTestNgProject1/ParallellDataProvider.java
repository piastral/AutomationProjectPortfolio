package testNgPackage1.MavenTestNgProject1;

public class ParallellDataProvider {
	
/*we can run test cases parellely in TESTnG using atrribute parrallel in dataProvider paramter 
 * lets say we want to execute 100 test case we can specify that in dataprovider(parallel=true)
 * but running 100 test cases parallely will create network congestion 
 * to override that we can thread count in xml file on suite level tag and let the dataprovider
 * to choose how much thread count we can include lets 5 so in that order the parallel test case will run
 * 
 * 
 * 	
 */

}
